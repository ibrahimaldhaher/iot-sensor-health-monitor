# Runtime: the package itself has no third-party dependencies.
# These are needed only for the notebook and the test suite.
matplotlib>=3.7
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
jupyter>=1.0
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
